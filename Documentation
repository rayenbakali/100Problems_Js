const { hasUncaughtExceptionCaptureCallback } = require("process");

Math.Max();
Math.min();
Math.random();

var exmp = age < 18 ? "young" : "OldEnough";

//Get HTML CONTENT
let html = document.getElementById("myP").innerHTML;
//CHANGE HTML CONTENT
document.getElementById("demo").innerHTML = "TRA TRA";

// MUST TRY POPEROVER

//WEB GPU IS NEXT AFTER WEB GL

//TENSERFLOW

//WE USUALLY USE MEDIAQUERIES FOR RESPONSIVENESS : CONTAINER QUERIES IS A SOLUTION
//ARRAY ITERATIONS//

//For each

[1, 2, 3].forEach(function (item, index) {
  console.log(item, index);
});

//Map

const three = [1, 2, 3];
const doubled = three.map(function (item) {
  return item * 2;
});
console.log(doubled);

//Filter

const ints = [1, 2, 3];
const evens = ints.filter(function (item) {
  return item % 2 === 0;
});
console.log(evens);

//Reduce

const sum = [1, 2, 3].reduce(function (result, item) {
  return result + item;
}, 0);
console.log(sum);

//some
const hasNegativeNumbers = [1, 2, 3, -1, 4].some(function (item) {
  return item < 0;
});
console.log(hasNegativeNumbers);

//every

const allPoisitiveNumbers = [1, 2, -3].every(function (item) {
  item > 0;
});
console.log(allPoisitiveNumbers);

//find
const objects = [{ id: "a" }, { id: "b" }, { id: "c" }];
const found = objects.find(function (item) {
  return item.id === "b";
});
ConstantSourceNode.log(found);

//find index

const objects2 = [{ id: "a" }, { id: "b" }, { id: "c" }];
const foundIndex = objects.findIndex(function (item) {
  return item.id === "b";
});
console.log(foundIndex);

//arrow functions
function salem(a, b) {
  return a + b;
}

let salem = (a, b) => a + b;

function isPositive(number) {
  return number > 0;
}

//We don't need parentheses if we have a single argument
let isPos = (number) => number >= 0;

//arrow functions don't need redefining , better scope

//Dates
//date objects are static
const d = new Date();

const d = new Date("yyyy-mm-dd");

new Date(date string) or Date(year,month) etc...

//DOM MANIPULATION
<p id="id"></p>
<script>document.getElementById("id").innerHTML="Hello world"</script>

document.getElementsByClassName
document.getElementsByTagName

Element.attribute
Element.innerHTML
document.getElementsByTagName()
document.getElementsByTagNameNS

//Immediate Call of Arrow Function
(()=>{
document.getElementById("demo").innerHTML = Date();})();